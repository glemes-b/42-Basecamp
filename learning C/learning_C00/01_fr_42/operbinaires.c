#include <stdio.h> 

int main ()

	{
	char c;
	char c2;
	c =10;
	c2=12;

	printf(sizeof(c));

// aqui para inverter os valores binario (a ~ b) para & ou 	Operador	Descrição	Exemplo	Resultado&	AND bit-a-bit	A & B 0000 0001 (1) |	OR bit-a-bit	A | B	0111 0011 (115)^	XOR bit-a-bit	A ^ B	0111 0010 (114) ~	complemento de 1	~A	1110 1110 (238) <<	desloca à esquerda N bits  	A << 2	0100 0100 (68)>>	desloca à direita N bits	A >> 2	0000 0100 (4)
	}
