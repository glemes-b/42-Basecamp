#include <stdio.h>

int main(int argc, char *argv[])
{
	if(argc > 1)
	{
		int i;
		int tab[15]
		for (i = 1; i < argc; i++)
		printf("%s ", argv[0]);
	}
	else
	printf("nao foram passads argumentos corretos\n");
	return 0;
}